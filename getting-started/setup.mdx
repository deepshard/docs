---
title: Setup the Truffle SDK
description: "Things you'll need to install the SDK"
---

### Prerequisites & Setup

Before you install the Truffle SDK, please ensure your environment is set up correctly. This helps avoid common issues and keeps your Python projects isolated and manageable.

#### 1. Python 3.10+

<Tabs>
  <Tab title="Ubuntu / Debian">
    ```bash
    sudo apt update
    sudo apt install -y make build-essential libssl-dev zlib1g-dev \
      libbz2-dev libreadline-dev libsqlite3-dev wget curl llvm \
      libncursesw5-dev xz-utils

    python3 --version
    ```
  </Tab>
  <Tab title="macOS">
    ```bash
    brew install python@3.10
    python3 --version
    ```
  </Tab>
  <Tab title="Windows (PowerShell)">
    ```powershell
    choco install python --version=3.10.0
    python --version
    ```
  </Tab>
</Tabs>

<Warning>Make sure you have Python 3.10 or higher installed before proceeding.</Warning>

#### 2. Create a Virtual Environment

A virtual environment keeps your dependencies isolated. Run:

```bash
python3 -m venv truffle-env
```

Activate it:

<Tabs>
  <Tab title="Linux / macOS">
    ```bash
    source truffle-env/bin/activate
    ```
  </Tab>
  <Tab title="Windows (PowerShell)">
    ```powershell
    truffle-env\Scripts\Activate.ps1
    ```
  </Tab>
</Tabs>

#### 3. Install the Truffle SDK

With your venv active, run:

```bash
pip install truffle-sdk
```

<Tip>Your Python environment is now ready. You can start building with the Truffle SDK!</Tip>
